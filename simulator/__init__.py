"""Baseball game simulation package.

Phase 4 will populate this package with Monte Carlo simulation steps.
Until then, this file exists so the directory is tracked in git and the
Docker build context includes it for ``COPY simulator/ ./simulator/``.
"""
