# =============================================================================
# MLB Baseball Simulation Platform — Development & Test Dependencies
# Python 3.13+  (SIM-431)
#
# Install: pip install -r requirements-dev.txt
# (includes all runtime deps via -r requirements.txt)
# =============================================================================

-r requirements.txt

# ---------------------------------------------------------------------------
# Testing framework
# ---------------------------------------------------------------------------
pytest>=8.0,<9.0
pytest-asyncio>=0.23,<1.0       # async test support (asyncpg fixtures)
pytest-cov>=5.0,<6.0            # coverage reporting
pytest-timeout>=2.0,<3.0        # guard against hung integration tests
pytest-benchmark>=4.0,<5.0      # performance benchmarks (simulation throughput SLA)
pytest-mock>=3.14,<4.0          # unittest.mock integration

# ---------------------------------------------------------------------------
# Integration test infrastructure
# ---------------------------------------------------------------------------
testcontainers[postgres,redis]>=4.0,<5.0   # ephemeral PostgreSQL + Redis per session
httpx>=0.27,<1.0                            # async HTTP test client for FastAPI

# ---------------------------------------------------------------------------
# Linting & type checking
# ---------------------------------------------------------------------------
ruff>=0.4,<1.0                  # fast linter + formatter (replaces flake8 + black)
mypy>=1.10,<2.0                 # static type checker
types-requests>=2.31,<3.0       # stubs for requests
types-redis>=4.6,<5.0           # stubs for redis

# ---------------------------------------------------------------------------
# Property-based testing
# ---------------------------------------------------------------------------
hypothesis>=6.100,<7.0          # property-based tests for similarity engine edge cases

# ---------------------------------------------------------------------------
# Dev utilities
# ---------------------------------------------------------------------------
ipython>=8.0,<9.0               # interactive debugging
rich>=13.0,<14.0                # pretty tracebacks + progress bars
